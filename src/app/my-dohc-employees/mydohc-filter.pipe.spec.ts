import { MydohcFilterPipe } from './mydohc-filter.pipe';

describe('MydohcFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MydohcFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
